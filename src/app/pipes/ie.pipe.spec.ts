/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { IePipe } from './ie.pipe';

describe('Pipe: Iee', () => {
  it('create an instance', () => {
    const pipe = new IePipe();
    expect(pipe).toBeTruthy();
  });
});
